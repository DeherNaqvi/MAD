import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  valueText: {
    fontSize: 16,
    color: '#222',
    marginTop: 4,
  },
  narrowButton: {
    width: '60%',
    marginVertical: 5,
    borderRadius: 15,
  },
});
